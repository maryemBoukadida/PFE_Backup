/*const bcrypt = require("bcrypt");

const generateHash = async() => {
    const password = "123456"; // mot de passe que tu veux
    const hash = await bcrypt.hash(password, 10);
    console.log("Mot de passe hashé :", hash);
};

generateHash();
*/